import setuptools

setuptools.setup(
    name="get_min",
    version="0.0.1",
    author="Tomasz Kesik",
    author_email="tomkesik@gmail.com",
    description="It finds the minimum",
    long_description="It finds the minimum",
    packages=setuptools.find_packages(),
    install_requires=[
    ],
)
